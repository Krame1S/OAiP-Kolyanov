#pragma once
#include <stdio.h>
#include <time.h>
#include "array.h"
#include "output.h"

void bubble_sort(int* k, int* SIZE, int* array, int* FLAG);
void selection_sort(int* k, int* SIZE, int* array);
